{
  "name": "fsdt-example",
  "private": "true",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "run-p start:*",
    "start:client": "yarn --cwd client start",
    "start:server": "yarn --cwd server start",
    "start:fsdt-devtool": "yarn fullstack-devtool -p 1234",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\""
  },
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.1.1"
  },
  "dependencies": {
    "@fullstack-devtool/app": "0.5.1"
  }
}
